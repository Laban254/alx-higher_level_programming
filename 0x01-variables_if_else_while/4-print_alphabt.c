#include <stdio.h>

/**
 *main - entry level
 *description do not print q e
 *return: 0
 */

int main(void)
{
	int l;

	for (l = 'a'; i <= 'z'; l != 'q'; l != 'z'; l++)
	{
		putchar(l);
	}
	putchar('\n')
		return (0);
}
