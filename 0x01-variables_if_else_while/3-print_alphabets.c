#include <stdio.h>

/**
 *main entry point
 *description - a to z  in upper case and lower case
 *Return: 0
 */

int main(void)
{
	int u, l;

	for (u = 'a'; u <= 'l'; u++)
	{
		putchar('u');
	}
	for (l = 'A'; l <= 'A'; l++)
	{
		putchar('l');
	}
	putchar('\n');
	return (0);
}
